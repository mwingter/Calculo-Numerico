"""
Compilar e rodar Python:
- Abrir terminal
- No terminal, ir até a pasta que o arquivo python está
- Para rodar é só digitar o nome do arquivo, ex: nomedoarquivo.py
"""


h = input("Enter hours: ")
r = input("Enter rate: ")
p = float(h) * float(r)
print("Pay: ", p)